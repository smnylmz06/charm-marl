# CHARM-MARL Python dependencies.
# Google Research Football is installed separately (see README / Dockerfile),
# because it builds a C++ engine and needs system libraries.

torch>=1.10
numpy>=1.19
scipy>=1.7
faiss-cpu>=1.7.0
gym==0.21.0
gfootball
opencv-python
psutil
